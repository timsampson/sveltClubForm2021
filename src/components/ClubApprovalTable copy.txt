<script>
  import { onMount } from "svelte";
  let records = [];
  let approvals = [];
  let approved = [];
  let recordIndex = 0;
  onMount(() => {
    google.script.run.withSuccessHandler(showApprovalRecords).getClubsForApproval();
  });
  function showApprovalRecords(allUserApplicationRecords) {
    records = allUserApplicationRecords;
  }
  function handleSubmit() {}
  function clubApprovalResponse() {
    alert("Approvals Processed");
    console.table(approvals);
  }
</script>

<div class="mt-2 mx-auto p-4">
  <h2>Pending Records</h2>
  {#if records.length > 0}
    <form on:submit|preventDefault={handleSubmit} id="approvalForm">
      <ul>
        {#each records as record}
          <li class="ml-2 py-1 ">
            <input
              class:text-red-500={!record.hasCapacity}
              class:border-red-500={!record.hasCapacity}
              class:border-blue-500={record.hasCapacity}
              class:hover:bg-red-200={!record.hasCapacity}
              class:hover:bg-blue-200={record.hasCapacity}
              class:focus:ring-red-500={!record.hasCapacity}
              class:focus:ring-blue-500={record.hasCapacity}
              class="mr-2 border-2 rounded focus:outline-none focus:ring-2 focus:ring-opacity-80"
              type="checkbox"
              name="approvals[]"
              value={record.recordId}
              id={record.recordId}
            />
            <label
              class:text-red-500={!record.hasCapacity}
              class:italic={!record.hasCapacity}
              for={record.recordId}
            >
              {record.name} in homeroom {record.homeroom} grade
              {record.grade} would like to join the
              <strong>{record.hasCapacity ? "" : "full"}</strong>
              {record.appliedClubName} club.
            </label>
          </li>
        {/each}
      </ul>
      <br />
      <div>
        <button
          type="submit"
          formaction="={handleSubmit}"
          class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg inline-flex items-center mr-2"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-5 w-5"
            viewBox="0 0 20 20"
            fill="currentColor"
          >
            <path
              fill-rule="evenodd"
              d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
              clip-rule="evenodd"
            />
          </svg>
          <span class="ml-2">Approve</span>
        </button>
        <button
          type="submit"
          formaction="={handleSubmit}"
          class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg inline-flex items-center ml-2"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-5 w-5"
            viewBox="0 0 20 20"
            fill="currentColor"
          >
            <path
              fill-rule="evenodd"
              d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z"
              clip-rule="evenodd"
            />
          </svg>
          <span class="ml-2">Reject</span>
        </button>
      </div>
    </form>
  {:else}
    <p>No records needing approval</p>
  {/if}
</div>
